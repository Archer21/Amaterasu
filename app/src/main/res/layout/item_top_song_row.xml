<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="172dp"
    android:orientation="vertical"
    app:cardElevation="2dp"
    app:cardPreventCornerOverlap="false">

    <ImageView
        android:id="@+id/top_song_image"
        android:src="@drawable/eair_aoi"
        android:scaleType="centerCrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/top_song_name"
            android:text="Ignite"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:padding="4dp"
            android:textColor="@color/textIcons"
            android:textSize="24sp" />

        <!--<RatingBar-->
            <!--android:id="@+id/top_song_rating"-->
            <!--android:numStars="5"-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="wrap_content" />-->

            <!--android:progressTint="@color/colorAccent"-->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="4dp"
            android:gravity="center_horizontal">

            <TextView
                android:id="@+id/top_song_playcount"
                android:text="5000"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginRight="8dp"
                android:drawableLeft="@drawable/ic_play"
                android:drawableStart="@drawable/ic_play"
                android:gravity="center"
                android:textColor="@color/colorAccent" />

            <TextView
                android:id="@+id/top_song_rating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="4.5"
                android:drawableLeft="@drawable/ic_listener"
                android:drawableStart="@drawable/ic_listener"
                android:gravity="center"
                android:textColor="@color/colorAccent" />

        </LinearLayout>

    </LinearLayout>
</android.support.v7.widget.CardView>


























